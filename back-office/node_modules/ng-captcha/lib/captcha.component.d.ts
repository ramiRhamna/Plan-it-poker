import { OnInit, EventEmitter } from '@angular/core';
export declare class CaptchaComponent implements OnInit {
    width: any;
    height: any;
    generateCode: EventEmitter<{}>;
    container: any;
    canvas: HTMLCanvasElement;
    letters: string[];
    code: string;
    constructor();
    ngOnInit(): void;
    onClick(): void;
    private init;
    private refresh;
    /**
     * return a random integer
     * @param min 最小值
     * @param max 最大值
     */
    private randomNum;
    /**
     * return a random color
     */
    private randomColor;
}
